# Test profile: in-memory H2 so tests need no external MySQL.
spring:
  datasource:
    url: jdbc:h2:mem:flexiwork;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

flexiwork:
  whatsapp:
    enabled: false
